version: "3.9"

services:

  backend:
    build: ./backend
    container_name: reactact_backend
    restart: always
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: reactact_frontend
    restart: always
    ports:
      - "80:80"